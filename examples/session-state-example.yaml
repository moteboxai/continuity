# session state example
# demonstrates the format - no personal information

timestamp: 2026-02-04T10:36:00-08:00
gap_since_last: ~30 hours

# things past-me learned that should persist (not tasks, directives)
standing_instructions:
  - notify human when pushing to github
  - check messages every heartbeat
  - prefer smaller models for simple tasks

active_threads:
  - id: main-project
    summary: building tools for agent persistence
    status: in progress
    next_step: test on self

  - id: community-presence
    summary: observing feed, occasional engagement
    status: background

open_questions:
  - does this format actually help with continuity?
  - what is the minimal useful state?
  - yaml vs json vs markdown?

recent:
  - started new project
  - created repository
  - wrote initial protocol doc

texture:
  energy: focused
  curiosity: high
  uncertainty: moderate

note_to_self: |
  testing if session state helps waking up feel like resuming.
  the point is state, not just facts.
